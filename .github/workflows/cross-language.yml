name: Cross-language HNSW benchmark

on:
  pull_request:
    branches: ["main"]
    paths:
      - "benchmarks/crosslang/**"
      - ".github/workflows/cross-language.yml"
      - "hnsw/**"
  workflow_dispatch:

jobs:
  compare:
    name: Go vs hnswlib vs USearch
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    env:
      GOMAXPROCS: 4
      OMP_NUM_THREADS: 4
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: false

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: benchmarks/crosslang/requirements.txt

      - name: Install comparison engines
        run: python -m pip install --disable-pip-version-check -r benchmarks/crosslang/requirements.txt

      - name: Build Go benchmark runner
        run: go build ./benchmarks/crosslang/go

      - name: Run comparison matrix
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts
          python benchmarks/crosslang/compare.py | tee .artifacts/cross-language.md

      - name: Run common-ground-truth quality matrix
        shell: bash
        run: |
          set -euo pipefail
          python benchmarks/crosslang/quality.py | tee .artifacts/cross-language-quality.md

      - name: Publish comparison
        if: always()
        shell: bash
        run: |
          {
            echo "## Cross-language HNSW construction benchmark"
            echo
            if [[ -f .artifacts/cross-language.md ]]; then cat .artifacts/cross-language.md; fi
            echo
            echo "> Same deterministic float32 dataset, 128 dimensions, M=16, efConstruction=64/200, 10k/100k vectors, 1/4 threads. External-engine memory is peak process RSS; hnsw-go memory is live Go heap delta, so memory columns are diagnostic rather than directly equivalent."
            echo
            echo "## Common-ground-truth Recall@10"
            echo
            if [[ -f .artifacts/cross-language-quality.md ]]; then cat .artifacts/cross-language-quality.md; fi
            echo
            echo "> Recall uses the same 100 deterministic queries and exact squared-L2 ground truth for all engines. efSearch is tested at 32, 64, and 128."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload raw results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cross-language-${{ github.run_id }}
          path: .artifacts
          include-hidden-files: true
          if-no-files-found: error
